# Setup Guide

See the [installation steps][install] below, and check the [FAQ][missing-ref]
for common problems — that reference is never defined.

There's also a dead anchor to [a heading that doesn't exist](#nonexistent-heading).

[install]: #installation

## Installation

Run the setup script.

[unused]: https://example.com/unused-reference
